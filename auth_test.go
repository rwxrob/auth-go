package auth

/*
// TODO later
func TestEncryptWithTouchAuth(t *testing.T) {
	buf := []byte(`something to secure`)
	enc, _ := EncryptWithTouchAuth(buf)
	t.Log(base64.StdEncoding.EncodeToString(enc))
}
*/
